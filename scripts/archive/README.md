# Archived one-off scripts

Recovery and patch scripts kept for history. Not used by `npm run` lifecycle.

Active scripts remain in `scripts/` (e.g. `build-assets.mjs`, `sync-from-main.mjs`, `export-help-content.mjs`).
