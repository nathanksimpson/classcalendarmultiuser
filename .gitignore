# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.cursor/

# Dependencies (if added later)
node_modules/

# Local recovery / duplicate artifacts
app.js.recovered-from-multi
styles (1).css
scripts/recover-log.txt
scripts/check-backup-out.txt
scripts/.seed-admin-temp.sql
scripts/.seed-admin-query.sql

# Env / secrets
.env
.env.*
!.env.example
.tmp-cookies.txt
.tmp-login-resp*.txt

# Team calendar database (created at runtime)
data/

# Production build output (npm run build)
dist/

# Cloudflare Wrangler local state
.wrangler/
.dev.vars
